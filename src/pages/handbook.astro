---
import { getCollection } from 'astro:content';
import { renderMarkdownWithToc } from '../lib/render-md';
import BaseLayout from '../layouts/BaseLayout.astro';

const entry = (await getCollection('handbook')).find(
  (e) => e.id === 'fde-handbook.md' || e.id === 'fde-handbook' || e.id.endsWith('/fde-handbook.md'),
);
const body = entry?.body ?? '';
const { html, toc } = await renderMarkdownWithToc(body);
---

<BaseLayout title="工作手册" activeSlug="handbook">
  <article class="handbook">
    <header class="handbook-head">
      <p class="eyebrow">可执行检查清单</p>
      <h1>FDE 工作手册</h1>
      <p class="lead">
        把《前线部署工程师》全书方法论提炼为按交付旅程组织的检查清单。
        每一阶段含核心信条、决策清单、高危信号(❗)、阈值标准与关键产出物。
        方括号可勾选；带 ❗ 为书中明确的高危信号或红线。
      </p>
    </header>

    {
      toc.length > 0 && (
        <nav class="handbook-toc" aria-label="手册目录">
          <p class="toc-title">目录</p>
          <ul>
            {toc.map((t) => (
              <li><a href={`#${t.id}`}>{t.text}</a></li>
            ))}
          </ul>
        </nav>
      )
    }

    <div class="handbook-body" set:html={html} />
  </article>
</BaseLayout>

<style is:global>
  .handbook { max-width: 50rem; }
  .handbook-head { margin-bottom: 2rem; }
  .eyebrow {
    font-size: 0.72rem; text-transform: uppercase; letter-spacing: 0.14em;
    color: var(--muted); font-weight: 400; margin: 0 0 0.6rem;
    font-family: var(--sans);
  }
  .handbook-head h1 {
    font-size: clamp(1.7rem, 1.2rem + 2vw, 2.3rem);
    font-weight: 400; margin: 0 0 0.8rem; letter-spacing: 0.01em;
    line-height: 1.2; color: var(--fg-strong);
  }
  .handbook-head .lead {
    font-size: 1rem; color: var(--muted);
    line-height: 1.7; margin: 0; max-width: 40rem;
  }
  .handbook-toc {
    background: var(--bg-side);
    border: 1px solid var(--border-soft);
    padding: 1rem 1.25rem;
    margin-bottom: 2.5rem;
  }
  .handbook-toc .toc-title {
    font-size: 0.72rem; font-weight: 400; text-transform: uppercase;
    letter-spacing: 0.14em; color: var(--muted); margin: 0 0 0.6rem;
    font-family: var(--sans);
  }
  .handbook-toc ul {
    list-style: none; padding: 0; margin: 0;
    columns: 2; column-gap: 1.5rem;
  }
  .handbook-toc li { margin: 0.25rem 0; break-inside: avoid; }
  .handbook-toc a { font-size: 0.88rem; color: var(--fg); }
  .handbook-toc a:hover { color: var(--accent); }
  @media (max-width: 600px) { .handbook-toc ul { columns: 1; } }

  .handbook-body { font-size: 0.95rem; line-height: 1.75; }
  .handbook-body :global(h2) {
    font-size: 1.3rem; font-weight: 400; margin: 2.5rem 0 1rem;
    padding-top: 1.5rem; border-top: 1px solid var(--border-soft);
    letter-spacing: 0.01em;
    scroll-margin-top: 4rem;
    color: var(--fg-strong);
    line-height: 1.25;
  }
  .handbook-body :global(h3) {
    font-size: 1.05rem; font-weight: 500; margin: 1.75rem 0 0.6rem;
    scroll-margin-top: 4rem;
    color: var(--fg-strong);
  }
  .handbook-body :global(h4) {
    font-size: 0.95rem; font-weight: 500; margin: 1.25rem 0 0.5rem;
    color: var(--accent);
  }
  .handbook-body :global(p) { margin: 0 0 0.85rem; }
  .handbook-body :global(blockquote) {
    border-left: 3px solid var(--border);
    padding: 0.2rem 0 0.2rem 1.1rem; margin: 1rem 0;
    color: var(--muted); font-size: 0.9rem; font-style: italic;
  }
  .handbook-body :global(ul), .handbook-body :global(ol) {
    margin: 0 0 1rem; padding-left: 1.4rem;
  }
  .handbook-body :global(li) { margin: 0.35rem 0; }
  .handbook-body :global(code) {
    font-family: var(--mono);
    font-size: 0.86em; background: var(--bg-side);
    padding: 0.08em 0.3em; border-radius: 2px;
  }
  .handbook-body :global(pre) {
    background: var(--bg-side); border: 1px solid var(--border-soft);
    padding: 1rem; overflow-x: auto;
    font-size: 0.82rem; line-height: 1.6;
  }
  .handbook-body :global(pre code) {
    background: none; padding: 0; font-size: inherit;
  }
  .handbook-body :global(table) {
    width: 100%; border-collapse: collapse; margin: 1rem 0;
    font-size: 0.88rem;
  }
  .handbook-body :global(th), .handbook-body :global(td) {
    border: 1px solid var(--border); padding: 0.5rem 0.75rem; text-align: left;
  }
  .handbook-body :global(th) {
    background: var(--bg-side); font-weight: 400;
    text-transform: uppercase; letter-spacing: 0.08em; font-size: 0.78rem;
    color: var(--muted);
  }
  .handbook-body :global(strong) { font-weight: 500; color: var(--fg-strong); }
  .handbook-body :global(hr) {
    border: none; border-top: 1px solid var(--border-soft);
    margin: 2rem 0;
  }
</style>
